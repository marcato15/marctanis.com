{
  "name": "gatsby-starter-default",
  "description": "Gatsby default starter",
  "version": "1.0.0",
  "author": "Kyle Mathews <mathews.kyle@gmail.com>",
  "dependencies": {
    "gatsby": "^1.9.223",
    "gatsby-link": "^1.6.38",
    "gatsby-plugin-react-helmet": "^2.0.6",
    "gatsby-plugin-styled-components": "^2.0.9",
    "react-helmet": "^5.2.0",
    "react-obfuscate": "^1.3.1",
    "react-stickynode": "^1.4.1",
    "styled-components": "^3.2.1"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gatsby build && yarn push",
    "push": "aws s3 sync public/ s3://marctanis.com/ && aws configure set preview.cloudfront true"
  },
  "devDependencies": {
    "prettier": "^1.11.1"
  }
}
